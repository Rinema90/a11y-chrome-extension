function changeFontFamily(font) {
    // change la police du body
  const newFontFamily = document.body;
  newFontFamily.style.fontFamily = font;
  // change la police h1
}

changeFontFamily("comic sans ms");

// changeFontFamily("Times New Roman");
